# KurweBall — Context Summary

## Session Summary
- **Date**: 2026-02-19
- **Project**: KurweBall — Unified Recruiting Platform (ATS + CRM)
- **Location**: `C:\Outlook Claude\Kurweball`
- **Branch**: master

### Accomplished This Session
1. **Bench Sales Module — Full CRUD** (built in earlier session, carried over)
2. **Add Record Dialog** — Manual data entry form with all fields in 2-column grid, Consultant required
3. **View/Edit Record Dialog** — Click any table row to open detail view showing all populated fields. Footer has Delete (red), Edit, Close buttons. Edit mode switches to editable form. Empty fields hidden in view mode.
4. **Horizontal Scrolling Fix** — Fixed table not scrolling when all columns enabled:
   - Added `min-w-0` to flex wrapper div and `<main>` in `app-shell.tsx`
   - Added `overflow-x-hidden` to `<main>` element
   - Changed from `<Table>` component to raw `<table className="w-max min-w-full">` to avoid nested scroll wrappers
   - Added `overflow-hidden` on Card, `overflow-x-auto` on scroll container
5. **Removed ALL mentor fields** — Removed `mentorsReview` and `mentorsEmail` from: Prisma schema, create DTO, service bulkImport, frontend (type, FORM_FIELDS, ALL_COLUMNS). Columns were already gone from DB. Now 32 fields total.
6. **Test Record** — "Rajesh Kumar" test record exists in DB (user may want to delete it)

### Key Files Modified This Session
- `apps/web/src/app/(dashboard)/bench-sales/page.tsx` — View/Edit dialog, clickable rows, horizontal scroll fix, removed mentor fields
- `apps/web/src/components/layout/app-shell.tsx` — Added `min-w-0` to flex containers, `overflow-x-hidden` to main
- `apps/api/prisma/schema.prisma` — Removed mentorsReview and mentorsEmail fields
- `apps/api/src/modules/bench-sales/dto/create-bench-sales.dto.ts` — Removed mentor fields
- `apps/api/src/modules/bench-sales/bench-sales.service.ts` — Removed mentor fields from bulkImport

### What Needs To Be Done Next
1. Delete the test "Rajesh Kumar" record if user wants
2. Clean up temp files (seed-bench-sales.ts, filter-bench.js, create-bench-excel.js, screenshot-*.png)
3. Update candidate detail/edit pages for new fields (applicantId, state, dateOfBirth, resumeAvailable)
4. Add bench sales export functionality
5. Deploy to Hetzner (65.108.218.222 — SSH port firewalled)

### Database Users
| Name | Email | Password | Role |
|------|-------|----------|------|
| Anvesh N | anvesh.n@engineeringsquare.us | Admin123$ | ADMIN |
| Mounica A (Hr) | Hr@engineeringsquare.us | Test1234$ | MANAGER |
| Saikumar | saikumar@engineeringsquare.us | Test1234$ | MANAGER |
| Sarah Johnson | sarah@acme.com | password123 | RECRUITER |
| Mike Davis | mike@acme.com | password123 | RECRUITER |

## Resume Instructions
1. Read this file + `memory/MEMORY.md`
2. Start API: `cd apps/api && node --require @swc-node/register src/main.ts`
3. Start frontend: `cd apps/web && npm run dev`
4. Bench Sales page at http://localhost:3000/bench-sales (has 1 test record — "Rajesh Kumar")
5. Both servers may still be running from this session
6. Next: user may want to delete test record, then clean up temp files
