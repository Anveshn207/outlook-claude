# ─── Database ───
POSTGRES_DB=kurweball
POSTGRES_USER=kurweball
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ─── API ───
DATABASE_URL=postgresql://kurweball:CHANGE_ME_STRONG_PASSWORD@postgres:5432/kurweball?schema=public
JWT_SECRET=CHANGE_ME_RANDOM_64_CHARS
JWT_REFRESH_SECRET=CHANGE_ME_ANOTHER_RANDOM_64_CHARS
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRY=7d

# ─── Email (optional — app works without it) ───
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@kurweball.com

# ─── Frontend ───
NEXT_PUBLIC_API_URL=/api

# ─── Caddy ───
DOMAIN=kurweball.yourdomain.com
